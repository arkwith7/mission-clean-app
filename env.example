# Mission Clean App - 환경 설정 예제 파일
# 이 파일을 .env로 복사하고 실제 값들로 수정하세요

# 도메인 설정
DOMAIN=aircleankorea.com
EMAIL=your-email@example.com

# Node.js 환경
NODE_ENV=production
PORT=3001

# 데이터베이스 설정 (SQLite)
DB_PATH=/app/mission_clean.sqlite
DB_NAME=mission_clean.db

# JWT 보안 설정 (REQUIRED - 반드시 강력한 값으로 변경하세요!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-at-least-32-characters-long
JWT_EXPIRES_IN=7d

# SSL/HTTPS 설정
SSL_CERT_PATH=/etc/letsencrypt/live/aircleankorea.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/aircleankorea.com/privkey.pem

# 로깅 설정
LOG_LEVEL=info
LOG_FILE_ENABLED=true

# API 문서 설정 (개발: true, 프로덕션: false)
SWAGGER_ENABLED=false

# CORS 설정 (프로덕션에서는 실제 도메인으로 제한)
CORS_ORIGIN=https://aircleankorea.com,https://www.aircleankorea.com

# 기본 관리자 계정 설정 (선택사항 - 보안상 권장하지 않음)
# ADMIN_EMAIL=admin@aircleankorea.com
# ADMIN_PASSWORD=
# MANAGER_EMAIL=manager@aircleankorea.com
# MANAGER_PASSWORD= 

# 기사 연락처 설정
TECHNICIAN_PHONE=010-9171-8465

# SMS 서비스 설정 (네이버 클라우드 SENS)
# SMS_ENABLED=false  # true로 설정하면 실제 SMS 발송
# NCLOUD_ACCESS_KEY=NCPxxxxxxxxxxxxxxxxxxxxxx        # 콘솔 → 마이페이지 → 인증키 관리
# NCLOUD_SECRET_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx  # ⚠️ 한번만 표시되므로 주의!
# NCLOUD_SENS_SERVICE_ID=ncp:sms:kr:xxxxxxxxxx:xxxxxxxx     # SMS 서비스 생성시 발급
# NCLOUD_SENS_FROM=010-9171-8465  # 등록된 발신번호

# 기본 관리자 계정 설정 (선택사항 - 보안상 권장하지 않음)
# DEFAULT_ADMIN_PASSWORD=custom_admin_password
# DEFAULT_MANAGER_PASSWORD=custom_manager_password
# DEFAULT_CUSTOMER_PASSWORD=custom_customer_password 